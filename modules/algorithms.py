from typing import List

class SortingAlgorithms():
    def __init__(self) -> None:
        pass

    
    def selection_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def bubble_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def insertion_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def merge_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def quick_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def heap_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def counting_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def radix_sort(self, data: List[int]) -> List[int]:
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def bucket_sort():
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def bingo_sort():
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError
    
    
    def shell_sort():
        """Sorts a list using the selection sorting algorithm.

        Args:
            `data (List[int])`: The iterable you want sorted.

        Returns:
            `List[int]`: A sorted list.
        """
        raise NotImplementedError